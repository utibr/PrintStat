@model PrintStat.Models.ViewModels.СonsumableView
@using (Html.BeginForm("EditСonsumable", "Сonsumable", FormMethod.Post, new { @class = "form-horizontal" }))
{
    <fieldset>
        <div class="control-group">
            <label class="control-label" for="TypeСonsumableID">
                Тип комплектующего
            </label>

            <div class="styled-select">
                @Html.DropDownList("TypeConsumableID", new SelectList(ViewBag.TypeConsumable, "ID", "Name"))
                @Html.ValidationMessage("TypeConsumableID")
            </div>

            <label class="control-label" for="Name">
                Наименование
            </label>

            <div class="controls">
                @Html.TextBox("Name", Model.Name)
                @Html.ValidationMessage("Name")
            </div>

            <label class="control-label" for="Endurance">
                Рабочий ресурс
            </label>
            <div class="controls">
                @Html.TextBox("Endurance", Model.Endurance)

            </div>

            <label class="control-label" for="ShortName">
                Краткое название
            </label>
            <div class="controls">
                @Html.TextBox("ShortName", Model.ShortName)

            </div>
        </div>

        <div class="form-actions">
            <button type="submit" class="btn btn-primary">
                Изменить
            </button>
            @Html.ActionLink("Отмена", "Index", null, null, new { @class = "btn" })
        </div>
    </fieldset>
}