@model IList<PrintStat.PaperType>
@{
    ViewBag.Title = "Типы бумаги";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Типы бумаги</h2>

<p>
    <div class="menu">
        <a href="@Url.Action("CreatePaperType", "PaperType")">Добавить тип бумаги</a>
    </div>

</p>

<p>
    <table>
        <thead>
            <tr>
                <th>Наименование</th>

                <th>Редакция</th>
                <th>Удаление</th>
            </tr>
        </thead>
        @foreach (var papertype in Model)
        {
            <tr>
                <td width="200">
                    @papertype.Name
                </td>

                
                <td width="80">
                    <a href="@Url.Action("EditPaperType", "PaperType", new { id = @papertype.ID })">●</a>
                </td>
                <td width="80">
                    <a href="@Url.Action("DeletePaperType", "PaperType", new { id = @papertype.ID })"><font color="red">●</font></a>
                </td>
            </tr>
        }
    </table>
</p>



