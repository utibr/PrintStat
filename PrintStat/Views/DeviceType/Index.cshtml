@model IList<PrintStat.DeviceType>
@{
    ViewBag.Title = "Виды устройств";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Виды устройств</h2>



<p>
    <div class="menu">
        <a href="@Url.Action("CreateDeviceType", "DeviceType")">Добавить вид устройства</a>
    </div>

</p>

<p>
    <table>
        <thead>
            <tr>
                <th>Наименование</th>

                <th>Редакция</th>
                <th>Удаление</th>
            </tr>
        </thead>
        @foreach (var devicetype in Model)
        {
            <tr>
                <td width="200">
                    @devicetype.Name
                </td>

                <td width="80">
                    <a href="@Url.Action("EditDeviceType", "DeviceType", new { id = @devicetype.ID })">●</a>
                </td>
                <td width="80">
                    <a href="@Url.Action("DeleteDeviceType", "DeviceType", new { id = @devicetype.ID })"><font color="red">●</font></a>
                </td>
            </tr>
        }
    </table>
</p>


